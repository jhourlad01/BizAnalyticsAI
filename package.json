{
  "name": "biz-analytics-ai-root",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2"
  },
  "lint-staged": {
    "client/**/*.{js,jsx}": [
      "npm run lint --prefix client",
      "npm run test -- --run --prefix client"
    ]
  }
}
